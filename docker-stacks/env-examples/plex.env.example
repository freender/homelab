# Host-local runtime .env for the plex stack (tower only).
# Never committed with real values -- copy to /mnt/cache/appdata/plex/.env
# on the host and fill in real values. docker-stacks does not manage this file
# (see docker-stacks/README.md).

DOMAIN="example.net"
