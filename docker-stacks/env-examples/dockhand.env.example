# Host-local runtime .env for the dockhand stack (helm only).
# Never committed with real values -- copy to /mnt/cache/appdata/dockhand/.env
# on the host and fill in real values. docker-stacks does not manage this file
# (see docker-stacks/README.md).

PUID="1000"
PGID="1000"
