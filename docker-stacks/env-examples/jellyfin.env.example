# Host-local runtime .env for the jellyfin stack (tower only).
# Never committed with real values -- copy to /mnt/cache/appdata/jellyfin/.env
# on the host and fill in real values. docker-stacks does not manage this file
# (see docker-stacks/README.md).

DOMAIN="example.net"
