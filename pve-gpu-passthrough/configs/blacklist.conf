# Blacklist GPU drivers on the host so devices can bind to vfio-pci.
# This is intentional for passthrough nodes (no local console output expected).

blacklist i915
blacklist nvidia
blacklist nouveau
