# /etc/homelab-http-boot/http-boot-mgmt.conf - network boot management configuration.
# Written by homelab deploy; do not edit manually.

# PDM answer server URL and TLS fingerprint
PDM_URL="{{ PDM_URL }}"
PDM_CERT_FINGERPRINT="{{ PDM_CERT_FINGERPRINT }}"

# HTTP Boot server management IP
HTTP_BOOT_MGMT_IP="{{ MGMT_IP }}"
