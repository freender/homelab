[Unit]
Description=Watch PDM completed auto-installs and queue homelab deploys
After=network-online.target homelab-postinstall-webhook.service
Wants=network-online.target

[Service]
Type=oneshot
EnvironmentFile=/etc/homelab-postinstall-webhook/env
ExecStart=/usr/local/sbin/homelab-pdm-installation-watch
