# Exact versions for every environment that runs ./validate: CI and the repo .venv.
# pyproject.toml holds the ranges; this file holds the one resolution both sides test.
# Without it the two drifted silently (pytest-cov 7.1.0 locally vs 6.3.0 in CI, with
# different subprocess-coverage hooks), so a CI failure could not be reproduced here.
#
# Install:     python -m pip install -c constraints.txt '.[dev,mutation]'
# Regenerate:  clean python3.13 venv, same install without -c, then
#              pip freeze --exclude homelab --exclude pip
# tests/test_dev_constraints.py fails when the running interpreter disagrees.
bcrypt==5.0.0
cffi==2.1.1
click==8.5.0
coverage==7.16.1
cryptography==50.0.1
decorator==5.3.1
Deprecated==1.3.1
fabric==3.2.3
iniconfig==2.3.0
invoke==2.2.1
Jinja2==3.1.6
libcst==1.9.0
linkify-it-py==2.2.0
markdown-it-py==4.2.0
MarkupSafe==3.0.3
mdit-py-plugins==0.6.1
mdurl==0.1.2
mutmut==3.8.0
packaging==26.3
paramiko==5.0.0
platformdirs==4.11.8
pluggy==1.6.0
pycparser==3.0
Pygments==2.21.0
PyNaCl==1.6.2
pytest==8.4.2
pytest-cov==6.3.0
PyYAML==6.0.3
PyYAML-ft==8.0.0
rich==13.9.4
ruff==0.11.13
setproctitle==1.3.7
textual==6.2.1
typing_extensions==4.16.0
wrapt==2.4.1
